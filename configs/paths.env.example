#!/usr/bin/env bash
# Set this root to a location with enough space for your own authorized data and outputs.
: "${TPUS_DATA_ROOT:?Set TPUS_DATA_ROOT before sourcing this file}"

export nnUNet_raw="${TPUS_DATA_ROOT}/nnUNet_raw"
export nnUNet_preprocessed="${TPUS_DATA_ROOT}/nnUNet_preprocessed"
export nnUNet_results="${TPUS_DATA_ROOT}/nnUNet_results"
